import { Body, Controller, Get, Param, ParseUUIDPipe, Patch, Post, UseGuards } from '@nestjs/common';
import { CurrentUser } from '../auth/decorators/current-user.decorator.js'; import { Roles } from '../auth/decorators/roles.decorator.js'; import type { AuthenticatedUser } from '../auth/auth.types.js'; import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard.js'; import { RolesGuard } from '../auth/guards/roles.guard.js';
import { CreateRatingDto } from './dto/create-rating.dto.js'; import { UpdateRatingDto } from './dto/update-rating.dto.js'; import { RatingsService } from './ratings.service.js';
@Controller('ratings') @UseGuards(JwtAuthGuard, RolesGuard) @Roles('USER')
export class RatingsController { constructor(private readonly ratingsService: RatingsService) {} @Post() create(@CurrentUser() user: AuthenticatedUser, @Body() dto: CreateRatingDto) { return this.ratingsService.create(user.sub, dto); } @Patch(':storeId') update(@CurrentUser() user: AuthenticatedUser, @Param('storeId', ParseUUIDPipe) storeId: string, @Body() dto: UpdateRatingDto) { return this.ratingsService.update(user.sub, storeId, dto); } @Get('me/:storeId') findOwn(@CurrentUser() user: AuthenticatedUser, @Param('storeId', ParseUUIDPipe) storeId: string) { return this.ratingsService.findOwn(user.sub, storeId); } }
